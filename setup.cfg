[mutmut]
paths_to_mutate=ableton_mcp/
backup=False
runner=python -m pytest -x --assert=plain
tests_dir=tests/
dict_synonyms=Struct, NamedStruct

[tool:pytest]
# Additional pytest settings for mutation testing
# Use -x to stop on first failure (faster mutation testing)
addopts = -x --tb=short
